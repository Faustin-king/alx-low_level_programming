#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the alphabets in lowercase
 *
 * followed by new line
 *
 * Return: Aways 0 (Success)
 */

int main(void)
{
	char alp;

	for (alp = a; alp <= z; alp++)
	{
		putchar(alp);
	}

	putchar('\n');


	return (0);
}
