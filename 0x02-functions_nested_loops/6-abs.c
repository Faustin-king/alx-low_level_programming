#include "main.h"

/**
 * _abs - Check main
 *
 * @n: An input
 * Decription: this function returns absolute value of a number
 * Return: Absolute value of number n
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}

		return (n * -1);
}
