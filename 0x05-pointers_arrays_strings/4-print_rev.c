#include "main.h"
#include <string.h>
#include <stdio.h>

/**
* print_rev - prints a string
* @s: An input character
* Followed by new line
*
* Return: Nothing
*/

void print_rev(char *s)
{
	int len;

	len = strlen(s);
}
