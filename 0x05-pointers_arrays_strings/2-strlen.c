#include "main.h"
#include <stdio.h>

/**
* _strlen - returns the length of a string
* @s: input charater
*
* Return: The length of a string
*/

int _strlen(char *s)
{
	int length;

	length = sizeof(s);

	return length;
}
